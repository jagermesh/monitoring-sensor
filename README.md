# Monitoring Sensor

## Setup

1) Add NPM package

```shell
npm init
npm install --save monitoring-sensor
```

## Metric data structure

{
  title: title,
  subTitle: subTitle,
  points: points,
  values: values,
  table: table
}
